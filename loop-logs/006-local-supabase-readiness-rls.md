# Loop 006 — Local Supabase Readiness + RLS Draft

## Goal

Prepare local Supabase execution by adding setup docs, checking/installing Supabase CLI prerequisites, attempting local stack execution when available, and drafting initial RLS helper functions and policies.

## Files created/updated

- `package.json` / `package-lock.json` — added Supabase CLI as a dev dependency via npm
- `supabase/config.toml` — generated by `npx supabase init --yes`
- `supabase/LOCAL_SETUP.md`
- `supabase/README.md`
- `supabase/migrations/20260626221000_rls_helpers_and_policies.sql`
- `GOAL.md`
- `loop-logs/006-local-supabase-readiness-rls.md`

## Prerequisite check

```text
npx supabase --version -> 2.108.0
docker -> not found / Docker daemon unavailable
psql -> not found
node -> v18.19.1
npm -> 9.2.0
```

## Local Supabase execution attempt

Command:

```bash
npx supabase start
```

Actual blocker:

```text
failed to inspect service: Cannot connect to the Docker daemon at unix:///var/run/docker.sock. Is the docker daemon running?
Docker Desktop is a prerequisite for local development. Follow the official docs to install: https://docs.docker.com/desktop
```

So the local Supabase stack cannot run in this environment yet. The CLI is installed and config exists, but Docker is the blocker.

## RLS helper/policy draft

Created:

```text
supabase/migrations/20260626221000_rls_helpers_and_policies.sql
```

Includes helper functions:

- `has_app_role(role)`
- `has_any_app_role(roles[])`
- `is_admin_operator()`
- `is_super_admin()`

Draft policies cover:

- profile reads/self-update/admin access
- profile role restrictions
- public reads for active/published entities
- admin/operator writes for admin-managed entities
- player self pre-registration insert/update
- tournament entry public reads
- add-on read/admin policies
- payout read/admin policies
- flight advancement admin/player read policies
- qualifier/TOC selector read/admin policies

## Verification commands run

```bash
python3 SQL file-level checks
npm --workspace packages/tournament-engine test
npm --workspace packages/tournament-engine run typecheck
npm --workspace packages/tournament-engine run build
npm audit --omit=dev
```

## Known limitations

- SQL was not executed against Postgres because Docker and `psql` are unavailable.
- RLS policy draft is intentionally broad for admin/operator roles and should be refined when host/manager assignment tables are designed.
- Seed profile UUIDs are not yet linked to real `auth.users` rows; that matters when testing `auth.uid()` locally.
- RPC signatures are still stubs/contracts, not implemented behavior.

## Next recommended loop

We have two reasonable options:

1. **If Docker is available/approved:** run `npx supabase start && npx supabase db reset`, fix migration/RLS execution issues.
2. **If Docker remains unavailable:** continue with app-layer preparation by scaffolding a Next.js/Supabase client prototype that consumes the pure tournament engine with mock data, while keeping DB work as SQL drafts.
