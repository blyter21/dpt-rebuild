import { AdminSectionHeader,AdminShell } from '../../../../components/admin'; import { NotificationsManager } from '../../../../components/notifications-manager'; import { getDptAdminSnapshot } from '../../../../lib/dpt-admin-repository'; export default async function Page(){const s=await getDptAdminSnapshot();return <AdminShell active="Notifications" source={s.source} generatedAt={s.generatedAt} repositoryMode={s.repositoryMode}><AdminSectionHeader title="Send Notifications">Targeted campaign preview and staging-only simulated delivery queue. No external provider transport exists.</AdminSectionHeader><NotificationsManager/></AdminShell>}
