import { AdminSectionHeader,AdminShell } from '../../../../components/admin'; import { Inbox } from '../../../../components/internal-inbox'; import { getDptAdminSnapshot } from '../../../../lib/dpt-admin-repository'; export default async function Page(){const s=await getDptAdminSnapshot();return <AdminShell active="Notifications" source={s.source} generatedAt={s.generatedAt} repositoryMode={s.repositoryMode}><AdminSectionHeader title="My Notifications">Private internal inbox. Marking a notification read is scoped to the signed-in profile.</AdminSectionHeader><Inbox/></AdminShell>}
