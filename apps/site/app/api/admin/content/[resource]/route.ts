import { NextRequest,NextResponse } from 'next/server';
import { dptAdminSupabaseFetch,getDptAdminApiContext } from '../../../../../lib/dpt-admin-api';
export const dynamic='force-dynamic';
const specs={
 articles:{table:'articles',rpc:'dpt_admin_save_article',deleteRpc:'dpt_admin_delete_article',select:'id,title,alias,status,category_id,tournament_id,published_at,featured,intro_html,body_html,video_url,seo_title,seo_description,article_profiles(profile_id)',fields:['title','alias','status','category_id','tournament_id','published_at','featured','intro_html','body_html','video_url','seo_title','seo_description','profile_ids']},
 categories:{table:'article_categories',rpc:'dpt_admin_save_category',deleteRpc:'dpt_admin_delete_category',select:'id,title,alias,status,parent_id,description,seo_title,seo_description,created_at',fields:['title','alias','status','parent_id','description','seo_title','seo_description']},
 emailtemplates:{table:'email_templates',rpc:'dpt_admin_save_email_template',copyRpc:'dpt_admin_copy_email_template',deleteRpc:'dpt_admin_archive_email_template',select:'id,title,subject,body_html,status,template_type,allowed_variables,updated_at',fields:['title','subject','body_html','status','template_type','allowed_variables']},
} as const;type Resource=keyof typeof specs;const isResource=(value:string):value is Resource=>value in specs;const object=(value:unknown):value is Record<string,unknown>=>Boolean(value)&&typeof value==='object'&&!Array.isArray(value);const exact=(value:Record<string,unknown>,keys:string[])=>Object.keys(value).every((key)=>keys.includes(key));
export async function GET(request:NextRequest,{params}:{params:{resource:string}}){const context=await getDptAdminApiContext();if(!context)return NextResponse.json({error:'Unauthorized'},{status:401});if(!isResource(params.resource))return NextResponse.json({error:'Unknown content resource'},{status:404});const spec=specs[params.resource];const q=request.nextUrl.searchParams,page=Math.max(1,Number(q.get('page')||1)),size=[10,25,50,100].includes(Number(q.get('size')))?Number(q.get('size')):10;const filters=[`order=${params.resource==='emailtemplates'?'updated_at':'id'}.desc`,`limit=${size}`,`offset=${(page-1)*size}`];if(q.get('search'))filters.push(`title=ilike.*${encodeURIComponent(q.get('search')!.trim())}*`);const list=dptAdminSupabaseFetch(context,`/rest/v1/${spec.table}?select=${encodeURIComponent(spec.select)}&${filters.join('&')}`,{headers:{Prefer:'count=exact'}});const optionCalls:Promise<Response>[]=[];if(params.resource==='articles'){optionCalls.push(dptAdminSupabaseFetch(context,'/rest/v1/article_categories?select=id,title&status=eq.true&order=title.asc'),dptAdminSupabaseFetch(context,'/rest/v1/tournaments?select=id,name&deleted_at=is.null&order=starts_at.desc&limit=500'));}else if(params.resource==='categories')optionCalls.push(dptAdminSupabaseFetch(context,'/rest/v1/article_categories?select=id,title&order=title.asc'));const [response,...optionResponses]=await Promise.all([list,...optionCalls]);if(!response.ok||optionResponses.some((item)=>!item.ok))return NextResponse.json({error:'Content list unavailable'},{status:502});const options=params.resource==='articles'?{categories:await optionResponses[0].json(),tournaments:await optionResponses[1].json()}:params.resource==='categories'?{categories:await optionResponses[0].json()}:{};return NextResponse.json({records:await response.json(),total:Number(response.headers.get('content-range')?.split('/')[1]||0),options});}
export async function POST(request:NextRequest,{params}:{params:{resource:string}}){const context=await getDptAdminApiContext();if(!context)return NextResponse.json({error:'Unauthorized'},{status:401});if(!isResource(params.resource))return NextResponse.json({error:'Unknown content resource'},{status:404});let body:Record<string,unknown>;try{body=await request.json() as Record<string,unknown>;}catch{return NextResponse.json({error:'Invalid JSON body'},{status:400});}const spec=specs[params.resource],id=body.id===null||body.id===undefined?null:body.id,validId=(typeof id==='number'&&Number.isSafeInteger(id)&&id>0)||(typeof id==='string'&&/^[0-9a-f-]{36}$/i.test(id));if(body.action==='save'&&exact(body,['action','id','values'])&&(id===null||validId)&&object(body.values)){const values=body.values;if(Object.keys(values).some((key)=>!spec.fields.includes(key as never)))return NextResponse.json({error:'Unexpected content field'},{status:400});if(typeof values.title!=='string'||!values.title.trim())return NextResponse.json({error:'Title is required'},{status:400});if(params.resource==='categories'&&typeof values.status!=='boolean')return NextResponse.json({error:'Invalid category status'},{status:400});if(params.resource==='articles'&&(typeof values.featured!=='boolean'||!['draft','published','archived'].includes(String(values.status))))return NextResponse.json({error:'Invalid article state'},{status:400});if(params.resource==='emailtemplates'&&(!['draft','published','archived'].includes(String(values.status))||!['system','user'].includes(String(values.template_type))||!Array.isArray(values.allowed_variables)))return NextResponse.json({error:'Invalid template state'},{status:400});const response=await dptAdminSupabaseFetch(context,`/rest/v1/rpc/${spec.rpc}`,{method:'POST',body:JSON.stringify({p_id:id,p_values:values})});if(!response.ok)return NextResponse.json({error:'Content save rejected',detail:await response.text()},{status:409});return NextResponse.json({record:await response.json()},{status:id?200:201});}const deleteRpc='deleteRpc'in spec?spec.deleteRpc:undefined,copyRpc='copyRpc'in spec?spec.copyRpc:undefined;if(body.action==='delete'&&deleteRpc&&exact(body,['action','id'])&&validId){const response=await dptAdminSupabaseFetch(context,`/rest/v1/rpc/${deleteRpc}`,{method:'POST',body:JSON.stringify({p_id:id})});if(!response.ok)return NextResponse.json({error:'Delete or archive blocked',detail:await response.text()},{status:409});return NextResponse.json({record:await response.json()});}if(body.action==='copy'&&copyRpc&&exact(body,['action','id'])&&validId){const response=await dptAdminSupabaseFetch(context,`/rest/v1/rpc/${copyRpc}`,{method:'POST',body:JSON.stringify({p_id:id})});if(!response.ok)return NextResponse.json({error:'Copy rejected',detail:await response.text()},{status:409});return NextResponse.json({record:await response.json()},{status:201});}return NextResponse.json({error:'Invalid content action'},{status:400});}
